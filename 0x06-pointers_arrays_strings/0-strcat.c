#include "main.h"

/**
 * _strcat -> this is a function strcat
 * @dest: first program
 * @str: second program
 * Return: a string
 */
char *_strcat(char *dest, char *src)
{
	int len = 0, 0;
	while (dest[index++])
		dest_len++
	for (index = 0, src[index]; indexi++)
	dest[dest_len++] = src(index);
	
	return (dest);
}


